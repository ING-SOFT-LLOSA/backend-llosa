# ===============================
# DATASOURCE
# ===============================

spring.datasource.url=${DB_URL_LLOSA}
spring.datasource.username=${DB_USERNAME_LLOSA}

spring.datasource.password=${DB_PASSWORD_LLOSA}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=${SHOW_SQL_LLOSA:false}
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# FLYWAY
# ===============================
# No se usa Flyway para el desarrollo raṕido. Enfocados en que funcione
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false
spring.flyway.baseline-version=0

firebase.service-account-path=${FIREBASE_CREDENTIALS_PATH}
app.dominio-corporativo=${DOMINIO_CORPORATIVO_LLOSA}