services:
  backend:
    build: .
    container_name: llosa_backend
    environment:
      - KEY1=${KEY1}
    networks:
      - proxy_net
    restart: unless-stopped

networks:
  proxy_net:
    external: true